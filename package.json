{
  "name": "easy-enum",
  "version": "1.1.2",
  "description": "Enums as easy as can be",
  "main": "index.js",
  "scripts": {
    "clean": "babel-node scripts/clean.js",
    "prebuild": "npm run clean",
    "build": "babel ./src -d ./",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [
    "enums"
  ],
  "author": "Steven Sojka",
  "license": "MIT",
  "devDependencies": {
    "babel-cli": "^6.5.1",
    "babel-core": "^6.5.2",
    "babel-plugin-transform-es2015-modules-commonjs": "^6.5.2",
    "babel-preset-es2015": "^6.5.0",
    "shelljs": "^0.6.0"
  }
}

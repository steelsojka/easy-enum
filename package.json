{
  "name": "easy-enum",
  "version": "1.1.2",
  "description": "Enums as easy as can be",
  "main": "index.js",
  "scripts": {
    "clean": "del dist",
    "prebuild": "npm run clean",
    "build": "tsc",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [
    "enums"
  ],
  "author": "Steven Sojka",
  "license": "MIT",
  "devDependencies": {
    "del-cli": "^0.2.1",
    "typescript": "^2.3.2"
  },
  "dependencies": {
    "tslib": "^1.7.0"
  }
}
